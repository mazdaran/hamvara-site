import test from 'node:test';import assert from 'node:assert/strict';import {initializeBackflush,updateBackflushQuantity,submitBackflush,approveBackflush} from '../../mrp/controlled-backflush.js';
const sample=()=>{const job={id:'J',workOrderNo:'WO',materials:[{sku:'RM',required:10}]};initializeBackflush(job);return{productionJobs:[job],stock:{RM:{'WH-SF':10}},auditTrail:[]}};
test('backflush is editable only before submission and checks WIP',()=>{const state=sample();updateBackflushQuantity(state,'J','RM',9);submitBackflush(state,'J',{user:'operator'});assert.equal(state.productionJobs[0].backflush.status,'SUBMITTED');assert.throws(()=>updateBackflushQuantity(state,'J','RM',8),/draft/)});
test('backflush requires an independent approver',()=>{const state=sample();submitBackflush(state,'J',{user:'operator'});assert.throws(()=>approveBackflush(state,'J',{user:'operator'}),/independent/);approveBackflush(state,'J',{user:'manager'});assert.equal(state.productionJobs[0].backflush.status,'APPROVED')});
