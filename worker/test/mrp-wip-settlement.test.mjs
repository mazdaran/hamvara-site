import test from 'node:test';import assert from 'node:assert/strict';import {normalizeFinancialState,createAccountingPeriod} from '../../mrp/financial-core.js';import {postProductionCostVariance} from '../../mrp/production-cost-variance.js';import {wipReconciliation,settleWipWorkOrder} from '../../mrp/wip-settlement.js';
function sample(){const state={settings:{currency:'TRY'},skus:[{code:'RM',cost:4}],stock:{RM:{'WH-SF':0}},productionJobs:[{id:'J',workOrderNo:'WO',batchNo:'B',status:'COMPLETED',plannedQty:10,completedQty:10,scrapQty:0,standardUnitCost:10,laborCost:0,overheadCost:0,materials:[{sku:'RM',required:20,standardUnitCost:4,actualQuantity:21,actualUnitCost:4}]}],operationSessions:[],operators:[],workCenters:[],auditTrail:[]};normalizeFinancialState(state);createAccountingPeriod(state,{startDate:'2026-09-01',endDate:'2026-09-30'});return state}
test('WIP settlement blocks until production variance is posted',()=>{const state=sample();assert.match(wipReconciliation(state,'J').issues.join(' '),/variance is not posted/);assert.throws(()=>settleWipWorkOrder(state,'J'),/variance is not posted/);postProductionCostVariance(state,'J',{date:'2026-09-11'});assert.equal(wipReconciliation(state,'J').readiness,'READY')});
test('settlement records a controlled immutable close',()=>{const state=sample();postProductionCostVariance(state,'J',{date:'2026-09-11'});const row=settleWipWorkOrder(state,'J',{user:'manager'});assert.equal(row.actualProductionCost,104);assert.equal(wipReconciliation(state,'J').readiness,'SETTLED');assert.throws(()=>settleWipWorkOrder(state,'J'),/already been settled/)});
